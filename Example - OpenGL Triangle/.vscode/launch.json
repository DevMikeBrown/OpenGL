{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch OpenGL App",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/UsingOpenGLInVSCODE.exe",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": true,
      "MIMode": "gdb",
      "miDebuggerPath": "gdb.exe", // Change if needed
      "setupCommands": [
        {
          "description": "Enable pretty printing",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ],
      "preLaunchTask": "cmake: build"
    }
  ]
}
